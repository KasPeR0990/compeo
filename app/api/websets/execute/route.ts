// to trigger the actual full webset search (after preview)
// actually runs the search, the bigger one after the preview with all updated filters and enrichments and return full results. 



// the execute, should check if the criteria and enrichments is matching, if an value is empty, display it at the bottom. it should always meet the criteria, but not all enrichments will be filled. 